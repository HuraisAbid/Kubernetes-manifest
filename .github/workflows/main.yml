name: CI/CD Pipeline

on:
  push:
    branches: [main]

env:
  FRONTEND_IMAGE: abidhurais/helm-manifest-frontend
  BACKEND_IMAGE: abidhurais/helm-manifest-backend
  NUMERIC_TAG: ${{ github.run_number }}

jobs:
# =====================================================
# 1. Detect Changes (SOURCE OF TRUTH)
# =====================================================
  detect-changes:
    runs-on: self-hosted
    outputs:
      frontend_changed: ${{ steps.detect.outputs.frontend }}
      backend_changed: ${{ steps.detect.outputs.backend }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Detect changed services
        id: detect
        shell: bash
        run: |
          set -euo pipefail

          FRONTEND=false
          BACKEND=false

          echo "🔍 Detecting changes..."

          # Determine diff range safely
          if git rev-parse HEAD~1 >/dev/null 2>&1; then
            DIFF_RANGE="HEAD~1 HEAD"
          else
            DIFF_RANGE="HEAD"
          fi

          echo "Diff range: $DIFF_RANGE"
          CHANGED_FILES=$(git diff --name-only $DIFF_RANGE || true)
          echo "$CHANGED_FILES"

          # Case-insensitive & folder-safe detection
          if echo "$CHANGED_FILES" | grep -qi '^frontend/'; then
            FRONTEND=true
          fi

          if echo "$CHANGED_FILES" | grep -qi '^backend/'; then
            BACKEND=true
          fi

          echo "frontend=$FRONTEND" >> "$GITHUB_OUTPUT"
          echo "backend=$BACKEND" >> "$GITHUB_OUTPUT"

          echo "✅ Frontend changed: $FRONTEND"
          echo "✅ Backend changed:  $BACKEND"

# =====================================================
# 2. Build Frontend (ONLY IF FRONTEND CHANGED)
# =====================================================
  build-frontend:
    needs: detect-changes
    if: needs.detect-changes.outputs.frontend_changed == 'true'
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Build & Push Frontend Image
        run: |
          docker build -t $FRONTEND_IMAGE:$NUMERIC_TAG ./frontend
          docker push $FRONTEND_IMAGE:$NUMERIC_TAG

# =====================================================
# 3. Build Backend (ONLY IF BACKEND CHANGED)
# =====================================================
  build-backend:
    needs: detect-changes
    if: needs.detect-changes.outputs.backend_changed == 'true'
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Build & Push Backend Image
        run: |
          docker build -t $BACKEND_IMAGE:$NUMERIC_TAG ./backend
          docker push $BACKEND_IMAGE:$NUMERIC_TAG

# =====================================================
# 4. Deploy Frontend (ONLY IF FRONTEND CHANGED)
# =====================================================
  deploy-frontend:
    needs: [detect-changes, build-frontend]
    if: needs.detect-changes.outputs.frontend_changed == 'true'
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Deploy Frontend via Helm
        run: |
          helm upgrade --install frontend helm/app \
            --namespace devops \
            --create-namespace \
            --set frontend.image=$FRONTEND_IMAGE \
            --set frontend.tag=$NUMERIC_TAG

# =====================================================
# 5. Deploy Backend (ONLY IF BACKEND CHANGED)
# =====================================================
  deploy-backend:
    needs: [detect-changes, build-backend]
    if: needs.detect-changes.outputs.backend_changed == 'true'
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Deploy Backend via Helm
        run: |
          helm upgrade --install backend helm/app \
            --namespace devops \
            --create-namespace \
            --set backend.image=$BACKEND_IMAGE \
            --set backend.tag=$NUMERIC_TAG
